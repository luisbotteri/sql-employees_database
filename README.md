# 📊 SQL Data Project: Employee Management (`employees_Table`)

## EN English Section: Data Diversity for SQL Practice

### ✨ Overview

This repository features a robust SQL data dump designed specifically for hands-on practice with **MySQL queries**. The core artifact is the `employees_Table`, populated with a large and varied dataset to simulate a realistic operational environment.

### 🚀 Key Features

| Feature | Details |
| :--- | :--- |
| **Record Count** | Approx. **450 rows** for comprehensive testing and aggregate function practice. |
| **Linguistic Diversity** | Names (`first_Name`, `last_Name`) include variations in both **Spanish and English**. |
| **Job Categorization** | Features over **17 distinct professional titles** (`job_Type`) to test `GROUP BY` and complex filtering. |
| **Status Field** | The `current_Status` column clearly separates `Active` employees from those who are `Unemployed` (where `job_Type` is set to `NULL`). |

### 🛠️ Getting Started (Import Instructions)

1.  **Clone or Download:** Get the `.sql` script from this repository.
2.  **Execute:** Run the script in your **MySQL Workbench** or preferred MySQL client.
3.  **Analyze:** The `employees_Table` is now fully populated and ready for immediate querying. Use it to showcase your SQL proficiency!

***

## 🇪🇸 Sección en Español: Práctica de Consultas con Diversidad de Datos

### ✨ Resumen del Proyecto

Este repositorio contiene un *dump* completo de datos SQL diseñado para la **práctica intensiva de consultas en MySQL**. El componente central es la tabla `employees_Table`, cargada con un amplio y variado conjunto de datos para simular una nómina operativa real.

### 🚀 Características Principales

| Característica | Detalles |
| :--- | :--- |
| **Cantidad de Registros** | Aproximadamente **450 filas** para pruebas exhaustivas y práctica de funciones de agregación. |
| **Diversidad Lingüística** | Los nombres y apellidos (`first_Name`, `last_Name`) incluyen variaciones tanto en **español como en inglés**. |
| **Categorización Laboral** | Incluye más de **17 títulos profesionales distintos** (`job_Type`) para probar `GROUP BY` y filtros complejos. |
| **Campo de Estatus** | La columna `current_Status` distingue claramente a los empleados `Active` de aquellos que están `Unemployed` (donde `job_Type` está en `NULL`). |

### 🛠️ Primeros Pasos (Instrucciones de Importación)

1.  **Clonar o Descargar:** Obtén el *script* `.sql` de este repositorio.
2.  **Ejecutar:** Ejecuta el *script* en tu **MySQL Workbench** o cliente MySQL de preferencia.
3.  **Analizar:** La tabla `employees_Table` está ahora completamente poblada y lista para consultas inmediatas. ¡Úsala para demostrar tu dominio de SQL!
